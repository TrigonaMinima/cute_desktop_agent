// AgentApp: the AppKit shell (overlay window, status item, display link, perception,
// avatar rendering). Placeholder until Phase 1+ lands — see native/README.md and the
// plan. Until then, `native/Sources/Spike` is the Phase 0 gate under test.

print("AgentApp: not yet implemented — see native/README.md (Phase 0 gate must pass first)")
